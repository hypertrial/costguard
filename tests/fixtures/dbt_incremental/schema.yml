version: 2

sources:
  - name: raw
    tables:
      - name: events

models:
  - name: stg_events
    tags: [staging]
    tests:
      - unique:
          column_name: id
    columns:
      - name: id
        tests:
          - not_null
  - name: fct_sessions
    tags: [mart, costguard_fixture]
    columns:
      - name: id
        tests:
          - not_null
