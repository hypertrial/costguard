version: 2

sources:
  - name: raw
    tables:
      - name: events

models:
  - name: stg_events
    columns:
      - name: id
        tests:
          - not_null
  - name: fct_sessions
    columns:
      - name: id
