# SQLCOST010: Python model row-wise operation

Detects row-wise dataframe patterns such as `.apply(axis=1)`, `.iterrows()`, and `.itertuples()`.

Prefer SQL, vectorized dataframe operations, or column-native transformations.
